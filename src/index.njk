---
layout: base.njk
title: Home
permalink: /
---

{% set labelNames = {
  "field-notes": "Field Notes",
  "judgment-calls": "Judgment Calls",
  "after-action": "After Action"
} %}

<section class="site-intro">
  <p class="lead">For riders who are past the honeymoon phase. Not here for gear lists or destination guides — here to think through what actually happens out there, and why.</p>
</section>

<section class="post-list">
  <h2 class="section-label">Recent Entries</h2>

  {% set recentPosts = collections.posts | reverse | limit(10) %}

  {% if recentPosts and recentPosts.length > 0 %}
    {% for post in recentPosts %}
    <article class="post-item">
      <div class="post-item-meta">
        {% if post.data.label %}
        <span class="post-label">{{ labelNames[post.data.label] }}</span>
        <span class="post-item-sep">·</span>
        {% endif %}
        <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
      </div>
      <h3 class="post-item-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      {% if post.data.description %}
      <p class="post-item-desc">{{ post.data.description }}</p>
      {% endif %}
    </article>
    {% endfor %}
  {% else %}
    <p class="empty-state">Nothing filed yet. Check back.</p>
  {% endif %}
</section>
